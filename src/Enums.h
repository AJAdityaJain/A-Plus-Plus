enum TokenType {
	NONE,

	BIT,
	INT,
	FLOAT,
	DOUBLE,
	STRING,

	ID,

	FUNC,
	IF,
	ELSE,
	WHILE,
	RETURN,

	BRACKET_OPEN,
	BRACKET_CLOSE,
	PARENTHESIS_OPEN,
	PARENTHESIS_CLOSE,
	CURLY_OPEN,
	CURLY_CLOSE,

	LINE_END,
	COMMA,
	COLON,
	ASSIGN,
	OPERATOR,

};

enum StatementType {
	NONE_STMT,
	BIT_STMT,
	INT_STMT,
	FLOAT_STMT,
	DOUBLE_STMT,
	STRING_STMT,
	REFERENCE,

	CALL,
	FUNC_DEFINITION,
	ASSIGNMENT,
	WHILE_STMT,
	IF_STMT,
	ELSE_STMT,
	MULTI_OPERATION,
	UN_OPERATION,
	SCOPE,
};

enum AssignmentType {
	NONE_ASSIGN,
	EQUALS,
	PLUS_EQUAL,
	MINUS_EQUAL,
	MULTIPLY_EQUAL,
	DIVIDE_EQUAL,

	MODULO_EQUAL,
	BITWISE_OR_EQUAL,
	BITWISE_AND_EQUAL,

};

enum UnaryOperatorType {
	NONE_UN_OPERATOR,
	NOT,
	BITWISE_NOT,
	POSITIVE,
	NEGATIVE,
};

enum MultipleOperatorType {
	NONE_BI_OPERATOR,

	OR,
	AND,
	BITWISE_OR,
	XOR,
	BITWISE_AND,

	COMPARISON,
	NOT_EQUAL,

	GREATER_THAN,
	SMALLER_THAN,
	GREATER_THAN_EQUAL,
	SMALLER_THAN_EQUAL,

	MINUS,
	PLUS,

	DIVIDE,
	MULTIPLY,
	MODULO,
};


enum InBuiltIds {
	MAIN,
	PRINT

};

enum Sizes {
	VOID_SIZE = 0,
	BIT_SIZE = 1,
	INT_SIZE = 4,
	FLOAT_SIZE = 4,
	DOUBLE_SIZE = 8,
	STRING_SIZE = 8,
};